class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        ans=[]
        for num in nums:
            sq=num*num
            ans.append(sq)
        ans.sort()
        return ans
        
